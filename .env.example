# Copy to .env and fill in — see src/lib/supabase.jsx.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional — CAPTCHA on the login form. Leave blank to disable entirely
# (login works normally either way). See src/lib/turnstile.js.
VITE_TURNSTILE_SITE_KEY=
